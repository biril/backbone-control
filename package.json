{
  "name": "backbone-control",
  "version": "0.0.1",
  "author": "biril <alexlambiris@gmail.com>",
  "description": "Common UI elements, reinvented as data-driven Backbone components",
  "contributors": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/biril/backbone-control"
  },
  "bugs": "https://github.com/biril/backbone-control/issues",
  "license": "MIT",
  "keywords": [
    "backbone",
    "UI",
    "controls"
  ],
  "dependencies": {
    "backbone": "^1.1.2",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "jshint": "^2.6.0",
    "less": "^2.3.1",
    "local-web-server": "^0.5.17"
  },
  "scripts": {
    "lint": "jshint --show-non-errors lib/*.js spec/*.js",
    "test": "bundle install && lessc --verbose test/Button/style.less test/Button/style.css && (ws -f none & WSPID=$! && rspec -fd test/Button/test.rb && kill $WSPID)"
  },
  "engines": {
    "node": ">=0.10"
  },
  "directories": {
    "lib": "lib"
  }
}
